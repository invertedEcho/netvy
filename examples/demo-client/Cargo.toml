[package]
name = "demo-client"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true, features = ["debug"] }
bevy-inspector-egui = "0.36.0"
bincode = { workspace = true }
netvy = { path = "../../crates/plugin/" }
