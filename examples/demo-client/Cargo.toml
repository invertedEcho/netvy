[package]
name = "demo-client"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true, features = ["debug"] }
bevy-multiplayer-plugin = { path = "../../crates/plugin/" }
bincode = { workspace = true }
