[package]
name = "netvy"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true }
bincode = { workspace = true }
netvy-server = { path = "../netvy-server", version = "0.1.0" }
shared = { path = "../shared/" }
